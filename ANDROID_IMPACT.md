# Android impact ledger

One line per change that the Android manager app (`/home/matimatik/AndroidStudioProjects/EdifyManager`)
must mirror. Consumed (and pruned back to this header) by the Android repo's `/sync-web` command.

Format: `- YYYY-MM-DD | <files> | <what the Android app must mirror>`

<!-- Synced through web commit 3ea8690 PLUS the uncommitted working tree of 2026-07-29
     (center IELTS groups: /api/manager/ielts-groups, IeltsOverviewTab, roster/settings
     twin mirroring, center_students-anchored add-to-group picker + multi-select,
     center_students-anchored dashboard student KPI) — consumed 2026-07-29.
     Deliberately NOT ported: the manager panel visual redesign and the uz/ru/en
     switcher (Android is Uzbek-only and has its own design system). -->
- 2026-07-31 — `lib/ielts/types.ts`, `services/ieltsService.ts`, `lib/ielts/importValidation.ts` (new), `app/admin/ielts/page.tsx`, `docs/IELTS.md`: **IELTS platform-catalog fields** — `ielts_test_meta` and the `ielts_*_tests` docs gained optional `catalogOrder` / `hiddenFromStudents` / `hiddenFromTeachers`, written only by the super-admin panel. **No-op for the manager app**: it reads neither the test banks nor `ielts_test_meta` (its IELTS surface is the group pair + the read-only overview API). Logged only because the ledger tracks every Firestore shape change. No rules change, no new index, no `/api/manager|directory|auth` change.
- 2026-08-03 — `types/Parent.ts` (new), `lib/parentLinks.ts` (new), `lib/server/parentReport.ts` (new), `app/api/manager/parent-links/route.ts` (new), `app/api/manager/parent-links/[token]/route.ts` (new), `app/api/parent/[token]/route.ts` (new), `services/parentLinkService.ts` (new), `app/manager/parents/*` (new), `app/manager/students/_components/ManagerStudentInfoPanel.tsx`, `app/manager/layout.tsx`, `firestore.rules` (§1.6), `tests/rules/parentLinks.rules.test.mjs` (new), `docs/PARENTS.md` (new): **account-free PARENT ACCESS by QR — a new manager nav item (`Ota-onalar`, between Students and Teachers) the Android manager app must mirror.** New collection `parent_links/{token}`: `{token, centerId, studentId, studentName, label, status:'active'|'revoked', scope:{results,levels,attendance,finance}, createdAt (epoch ms), createdBy, revokedAt?, lastViewedAt?, viewCount}`. ⚠️ **The document id IS the parent's credential** (24 chars from `23456789ABCDEFGHJKMNPQRSTWXYZ`, ≈117 bits, CSPRNG) and the collection is **`read, write: if false` for every client** — Android must NOT read or write it with the client SDK; use the three new manager endpoints: `GET /api/manager/parent-links[?studentId=]`, `POST /api/manager/parent-links {studentId,label?,scope?}`, `PATCH /api/manager/parent-links/{token} {status?,label?,scope?}` (all `requireActiveCenterManager`, Bearer ID token as usual). ⚠️ Every POST mints a NEW link (two parents → two revocable links); `studentId`/`centerId` are never patchable. The QR encodes `<origin>/p/{token}` — an ORIGIN-relative URL built from the device's own host, so a staging build must not hardcode the production domain. The parent side is a public web page (`/p/{token}` → `GET /api/parent/{token}`, no auth, 404 unknown / **410 revoked** / 429 rate-limited) — no Android work needed there, but the manager app should offer the same print/copy/revoke and the scope switches (⚠️ `finance` off means the report omits money entirely). No new index (list is `centerId ==`, sorted client-side); no change to any existing collection.
- 2026-08-03 — `types/Parent.ts`, `lib/parentLinks.ts`, `lib/server/parentReport.ts` (`claimParentLink`), `app/api/parent/[token]/route.ts`, `app/api/manager/parent-links/route.ts`, `app/api/manager/parent-links/[token]/route.ts`, `app/manager/parents/*`, `docs/PARENTS.md`, `docs/DATA_MODEL.md`: **parent access is now ONE CHILD ⇒ ONE CONNECTED PERSON, and shareable via Telegram/SMS** (amends today's earlier parent-access entry). TWO rules, both must be mirrored: (1) **`POST /api/manager/parent-links` REVOKES the student's other active links in the same batch** and returns `{link, replaced}` — the Android issue button must warn before pressing when the child already has a live link, and must not keep showing the old one afterwards; (2) **the first browser to open a link CLAIMS it** — `parent_links` gained `deviceHash?` (sha256 of a server-minted secret), `claimedAt?`, `claimedDevice?` ("iPhone · Safari"); `GET /api/parent/{token}` now takes header **`x-parent-device: <32-char secret>`**, returns the secret ONCE as `{report, device}` on the claiming request, and answers **409 `claimed`** to every other device (404 unknown / 410 revoked / 429 rate-limited unchanged). New manager action: **`PATCH /api/manager/parent-links/{token} {unbind:true}`** clears the three `claimed*` fields so the SAME printed QR can be re-claimed — that is the "parent changed phone" fix and the Android panel needs the button (label: *Qurilmani almashtirish*). The QR dialog also gained Telegram (`t.me/share/url`), SMS (`sms:?&body=`) and `navigator.share` delivery — on Android use the native `Intent.ACTION_SEND` share sheet instead of the web builders. ⚠️ Still no client-SDK access to `parent_links` (rules unchanged: `read, write: if false`) and still no composite index — the new replace query is `centerId ==` + `studentId ==` + `status ==`, three equality filters, verified to run index-free against the live project.
- 2026-09-04 — `functions/hikvision.js` (new), `functions/index.js` (new), `services/hikvisionService.ts` (new), `services/faceEventService.ts` (new), `components/attendance/FaceTerminalStatus.tsx` (new), `app/manager/face-config/page.tsx` (new), `app/manager/layout.tsx`, `app/manager/staff-attendance/page.tsx`, `app/manager/attendance/page.tsx`, `app/manager/students/page.tsx`, `components/attendance/StaffAttendanceGrid.tsx`, `types/attendance.ts`, `firestore.rules`: **Face ID terminal (Hikvision HikCentral Connect) check-in — new manager nav page `/manager/face-config`** (AK/SK config, connection test, per-staff/per-student face enrollment) that the Android manager app must mirror as a settings screen; keys are entered there and never exposed to any client (see below). New Cloud Functions (Admin SDK only, `functions/hikvision.js`): `hikSaveConfig`, `hikStatus` (masked keys + counts), `hikTest`, `hikEnrollFace`, `hikDevices` (terminal online/offline — polled every 60s by `FaceTerminalStatus`, now shown as a badge + "Face ID" link on **Staff attendance**, **Attendance** (groups tab), and **Students**), plus background `hikPollAttendance`/`hikWebhook` that write **new collection `face_events/{id}`**: `{centerId, staffUid, staffName, role:'teacher'|'student'|…, at (local ISO 'YYYY-MM-DDTHH:mm:ss'), kind:'checkin'|'checkout', source, processed}` (type: `types/attendance.ts` `FaceEventDoc`). The manager panel runs a global `listenFaceEvents(centerId)` listener (in `app/manager/layout.tsx`, active on every manager page) that live-processes unprocessed events into the EXISTING `center_staff_attendance` (staff) and `center_attendance` (student, via `checkInStudent({..., method:'face'})`) docs — no new attendance record shape, just a new writer path; `center_staff_attendance` docs gained a `method: 'manual'|'face'` field (grid shows a small face-icon badge on face-marked cells) that Android should read/display the same way. **Two more rules-locked collections, both `read, write: if false` for every client** — Android must never touch them directly: `_private/{configId}` (the AK/SK secrets) and `att_day/{dayId}` (Cloud-Function-only per-staff-per-day scan-state tracker, not consumed by any client code). `face_events` rules use the split `get` (strict: center manager or teacher-of-center)/`list: if isAuth()` pattern — same as `center_attendance`/`center_staff_attendance` — because the manager-panel listener is a range query; `update` (mark-processed) stays manager-only, `create`/`delete` are `false` (Cloud-Functions-only writes). No new composite index (`face_events` list query is `centerId ==` + `processed ==` + `orderBy(at)`, single-field). If/when the Android manager app adds face-terminal support, it should poll `hikDevices`/`hikStatus` the same way rather than reading Firestore directly for anything terminal-related.
- 2026-09-05 — `app/manager/finance/page.tsx`, `app/manager/finance/_components/PaymentKindSheet.tsx` (new), `app/manager/finance/_components/RecordTeacherPayoutModal.tsx` (new), `docs/FINANCE.md` (§9.2), `docs/FINANCE_DATABASE.md`: **"To'lov qabul qilish" now asks O'quvchi vs O'qituvchi first.** The FAB/PaymentsTab/StartGuide entry points open a new chooser sheet; picking O'qituvchi opens a teacher search → live current-month payroll breakdown → Pay, **always for the current calendar month**. This is UI-only — it calls the pre-existing `calculatePayrollApi`/`savePayoutApi`/`markPayoutPaidApi` (same `/api/manager/finance/payroll/*` routes the Oyliklar tab already uses) with no contract change, so Android only needs to mirror the UX (a kind-picker in front of the existing record-payment flow, and a teacher-payroll quick-pay screen reusing its existing payroll approve/mark-paid calls) — no new Firestore shape, no new endpoint, no rules change. Debtor-row and student-panel "To'lov" buttons are unaffected (they still go straight to the student flow).
- 2026-09-05 — `app/manager/finance/page.tsx`, `app/manager/finance/_components/ExportFinanceButton.tsx` (new), `lib/finance/exportExcel.ts` (new), `package.json` (new dep `xlsx` 0.18.5), `docs/FINANCE.md` (§9.3), `docs/FINANCE_DATABASE.md`: **new "Excelga eksport" button in the Moliya page header** (visible on every tab, revised same day from a fixed export into a picker) — opens a checklist of 5 sections (Hisob-kitob, To'lovlar, Qarzdorlar, Oyliklar, Xarajatlar; all checked by default), bundles only the checked ones into one `.xlsx`, client-side, read-only (no writes, no new API route, no Firestore shape change). 4 sections reuse data already on the page for the browsed month; Qarzdorlar uses the all-time `openCharges` snapshot (current debtors, not month-scoped — same data the Qarzdorlar tab shows); Oyliklar is the one fresh read, `calculatePayrollApi`, only called when checked. Delivery: `navigator.share({files})` on devices with Web Share Level 2 (Telegram shows up in the OS share sheet like any other installed app — there is deliberately no Telegram-bot backend/token), else a plain file download. Android has no direct equivalent yet — if/when the Android manager app adds this, mirror the picker (5 checkboxes, all-on default), generate the `.xlsx` on-device (e.g. Apache POI) from the same data sources (`payroll/calculate` response for Oyliklar, already-fetched charges/payments/expenses/openCharges for the rest), and use `Intent.ACTION_SEND` with the file `Uri` for the share sheet rather than reimplementing Telegram-specific logic.
- 2026-09-14 — `types/finance.ts` (`PaymentMethodSplit`, `Payment.methodSplit`, `Expense.method`/`Expense.methodSplit`, `RecordPaymentRequest.methodSplit`), `lib/server/financeOps.ts` (`normalizeMethodSplit`, `isPaymentMethod`), `app/api/manager/finance/payments/route.ts`, `app/api/manager/finance/expenses/route.ts`, `services/financeService.ts` (`createExpenseApi`), `app/manager/finance/_components/MethodSplitEditor.tsx` (new), `RecordPaymentModal.tsx`, `ExpensesTab.tsx`, `PaymentsTab.tsx`, `financeFormat.ts`, `lib/finance/exportExcel.ts`, `docs/FINANCE.md` (§4.2a), `docs/FINANCE_DATABASE.md`: **a payment or expense can now be split across 2+ methods in one entry** — e.g. 500,000 so'm as 300,000 by card + 200,000 cash. ⚠️ **`POST /api/manager/finance/payments` and `POST /api/manager/finance/expenses` both gained an optional body field `methodSplit: {method, amount, label?}[]`** — `method` stays `'cash'|'card'|'click'|'payme'|'transfer'|'other'`, `amount` a positive integer, `label` FREE TEXT (e.g. "AAA karta" — **no named-card/account registry exists**, it is typed fresh every time and snapshotted as-is). Server validates every line's method, that amounts sum to EXACTLY the request's own `amount` (else 400), and caps at 8 lines (`lib/server/financeOps.ts::normalizeMethodSplit`, shared by both routes). When `methodSplit` is present the stored scalar `method` is DERIVED (all lines same method → that method; mixed → `'other'`) rather than taken from the caller — so any reader that only knows the old scalar field still shows something sane. **Fully additive and backward-compatible**: omit `methodSplit` (or send none) and both endpoints behave byte-for-byte as before — an Android client that has never heard of this keeps working unchanged, on both write and read (`Payment.methodSplit`/`Expense.method`/`Expense.methodSplit` are all optional; absent = old-shape doc). If/when the Android manager app adds split entry, mirror: the request field name and per-line shape exactly, client-side sum validation before submit (server is authoritative but a mismatched sum should fail fast in the UI), and displaying `methodSplit` (when present) as "Method1 (label) amount1 + Method2 (label) amount2" in place of the single method label — see `formatMethodSplit()` in `financeFormat.ts` for the exact string shape to match. The Excel export's Xarajatlar sheet also gained a "Usul" column (previously had none) and the To'lovlar sheet's "Usul" column now shows the split summary instead of the raw enum value.
- 2026-09-14 — `firestore.rules` (new `school_classes` match block), `services/schoolClassService.ts` (new), `hooks/useSchoolClasses.ts` (new), `hooks/useCenterClasses.ts` (`ClassData.schoolClassId`/`schoolClassName`), `app/manager/groups/**` (redesigned — see below), `app/manager/teachers/_components/ManagerTeacherInfoPanel.tsx`, `app/manager/students/_components/ManagerStudentInfoPanel.tsx`, `app/manager/attendance/[classId]/page.tsx`, `app/manager/timetable/page.tsx`, `tests/rules/schoolClasses.rules.test.mjs` (new), `docs/MANAGER.md`, `docs/DATA_MODEL.md`, `docs/ATTENDANCE.md`, `docs/ROOMS.md`, `docs/FINANCE.md`: **"Groups" is now grade-based — a manager creates School Classes (e.g. "5-A") that hold several subject groups sharing one roster; mirror this whole flow.** New collection **`school_classes/{centerId}_{grade}_{section}`**: `{centerId, grade (string "1".."11"), section (string, uppercased), displayName, studentIds[] (ROSTER SOURCE OF TRUTH), createdAt, createdBy}` — the doc id is the uniqueness constraint (same idiom as `center_students`), rules-gated `isActiveCenterManager(centerId)`, `centerId`/`grade`/`section` immutable after create (no rename — delete-and-recreate). `classes` docs gained two **additive, optional** fields when they're a "subject" of a School Class: `schoolClassId` (back-link) and `schoolClassName` (denormalized `displayName`, e.g. "5-A", for cheap display) — **no other `classes` field or rule changed**, and finance/attendance/attempts backends need zero changes since a subject is still a plain `classes` doc with `centerId` set. ⚠️ **Roster invariant to mirror exactly**: adding/removing a student on a School Class must write `school_classes.studentIds` AND every subject `classes.studentIds` with matching `schoolClassId` in ONE batch (`syncSchoolClassRoster`) — same "keep the mirror in sync" contract as the `ielts_groups` twin, just N-way; a lone subject-only edit desyncs the shared roster. New Statistics tab (attendance + academic + finance rollup per class) is pure client aggregation, no new backend endpoint. **Migration is manual**: pre-existing flat groups are NOT auto-migrated — they show under a new "Unlinked groups" section on the Groups list with an "Attach to a class" action (unions the group's roster into the class, then fans it out). **IELTS groups are unaffected behaviorally**, just moved from `/manager/groups/{classId}` to `/manager/groups/detail/{classId}` (a routing-only change — Next.js can't have two different dynamic segment names as siblings — Android has no equivalent URL structure so this is web-only plumbing, not something to port). See the new "School Classes" section in `docs/MANAGER.md` for the full contract.
- 2026-09-15 — `types/office.ts` (new), `firestore.rules` (new `isCenterOffice()` helper, new `center_staff` + `center_staff_credentials` blocks, `|| isCenterOffice(...)` added to the READ rules of `center_finance_settings`/`center_student_finance`/`center_charges`/`center_payments`/`center_expenses`/`center_payouts`/`center_teachers`), `lib/server/verifyCenterOffice.ts` (new), `lib/server/financeRoute.ts` (`financePostHandler` gained an `options.office` argument), `app/api/manager/finance/{payments,payments/cancel,expenses,expenses/cancel,payroll/calculate}/route.ts`, `app/api/admin/centers/[id]/staff/route.ts` (new), `app/admin/centers/[id]/page.tsx` + `_components/StaffTab.tsx` (new), `app/office/*` (new), `services/officeService.ts` (new), `app/manager/finance/_components/{PaymentsTab,ExpensesTab,StudentInfoDialog}.tsx` (additive capability props), `app/manager/layout.tsx` (role redirect), `services/userService.ts` (`UserProfile.role` union), `tests/rules/centerStaff.rules.test.mjs` (new), `docs/OFFICE.md` (new), `docs/{AUTH,FINANCE,ADMIN,MANAGER,DATA_MODEL,README}.md`, `CLAUDE.md`: **two new center-scoped back-office roles — director and buxgalter (accountant) — with their own panel at `/office`.** ⚠️ **New collection `center_staff/{uid}`, doc id MUST equal the uid**: `{uid, centerId, staffRole: 'director'|'accountant', name, email, username, createdAt, createdBy}` — this is THE authorization anchor (the exact `center_teachers` idiom), `write: if false` for every client **including the manager**, written only by the Admin SDK. `users.role` gained the values `'director'`/`'accountant'` but they are a **REDIRECT HINT ONLY** — never authorize off them; the rules' `users` create allowlist still admits only student/teacher/manager. Also new: `center_staff_credentials/{uid}` (`{uid, centerId, password, updatedAt}`, rules `read, write: if false` — denied to EVERY client, manager included; god-mode/Admin-SDK only). **Rules reads widened, writes NOT**: every money collection stays `write: if false`; the new `isCenterOffice(centerId)` helper reads the CONSTANT path `center_staff/$(request.auth.uid)`, so a list query still costs a fixed number of document accesses (~3 for a director) — do not add more `get()`-based branches to those rules without recounting. ⚠️ **API contract change to mirror**: `POST /api/manager/finance/payments`, `/payments/cancel`, `/expenses`, `/expenses/cancel` now ALSO accept a caller who is an **accountant** of the center, and `/payroll/calculate` accepts ANY office role (it writes nothing) — resolution is manager-first, falling through to the office guard only on a **403** (a 401 propagates). Request/response shapes are byte-for-byte unchanged, so an existing Android manager client needs no change; `receivedBy`/`createdBy` on the resulting docs is the REAL caller's uid (an accountant's, not the manager's) — any Android UI that assumes those name the manager should stop assuming it. Every other finance route (`charges/*`, `settings`, `student`, `group-fees`, `payroll/save`, `payroll/mark-paid`, `teacher-salary`) stays manager-only. Provisioning is **super-admin only** (`GET|POST|PATCH|DELETE /api/admin/centers/{id}/staff`) — there is deliberately no `/api/manager/...` twin, so the Android manager app must NOT offer creating one; it may read its center's `center_staff` links (`centerId ==` filtered) to display who has back-office access. ⚠️ **Not yet done, mirror the gap too**: the admin cascade delete (`DELETE /api/admin/centers/[id]`) does NOT delete `center_staff`/`center_staff_credentials`. ⚠️ The new rules test suite `tests/rules/centerStaff.rules.test.mjs` **has not been run** (no Java on the dev machine → no emulator) — run `npm run test:rules` before trusting the deployed ruleset. Full contract: `docs/OFFICE.md`.
- 2026-09-16 — `types/finance.ts` (`ExpenseStatus` gains `pending_approval`/`rejected`, `Expense.approvedBy/At`, `Expense.rejectedBy/At/rejectReason`), `lib/server/financeOps.ts` (`createExpense` now takes `callerRole` and branches the starting status; `cancelExpense` widened to accept a self-withdraw of a `pending_approval` row; new `approveExpense`/`rejectExpense`), `lib/server/financeRoute.ts` (new `FinanceOfficeAccess` value `'director'`; `financePostHandler`'s op context gained `callerRole`), `app/api/manager/finance/expenses/approve/route.ts` (new), `app/api/manager/finance/expenses/reject/route.ts` (new), `services/financeService.ts` (`approveExpenseApi`, `rejectExpenseApi`; `createExpenseApi` now returns `{expenseId, status}`), `app/manager/finance/_components/ExpensesTab.tsx` (new `canApprove`/`currentUid` props), `app/manager/finance/page.tsx`, `app/office/page.tsx`, `types/office.ts` (`canApproveExpense`), `lib/finance/exportExcel.ts`, `docs/FINANCE.md` (§9), `docs/FINANCE_DATABASE.md`, `docs/OFFICE.md`: **expense recording now has an approval step when the BUXGALTER records it.** ⚠️ **`Expense.status` gained two values** — an expense a manager (or, unchanged, no one else previously) records still goes straight to `'active'`; one an **accountant** records now starts `'pending_approval'` and must be **approved** (`POST /api/manager/finance/expenses/approve {expenseId}`) or **rejected** (`POST /api/manager/finance/expenses/reject {expenseId, reason}`) by a manager or **director** before it counts as spent — the director's first WRITE capability anywhere (previously fully read-only). Both new routes are guarded `office: 'director'` (manager or director, **buxgalter excluded** — a submitter cannot approve their own entry), a THIRD `FinanceOfficeAccess` value beside the existing `'accountant'`/`'any'`. The submitter may withdraw their own still-pending expense via the EXISTING `/expenses/cancel` route (unchanged request shape) — `cancelExpense` now also accepts `status:'pending_approval'` but only when the caller is the original `createdBy`; the pre-existing `active`-only cancel path is byte-for-byte unchanged. ⚠️ **Every money-sum surface (stat cards, Xarajatlar/Foyda cards, Excel export total) filters the exact string `'active'`**, never `!== 'cancelled'` — so `pending_approval`/`rejected` rows are automatically excluded from totals with no extra filtering needed, but any Android UI that lists expenses must render the two new statuses (label them; do not assume only active/cancelled exist) and must gate its own approve/reject affordance to manager/director only, never buxgalter. `POST /api/manager/finance/expenses`'s request/response shape is otherwise unchanged except the response now includes `status` (was previously just `{expenseId}`) — an Android client that ignores unknown response fields needs no change to keep working, but should read `status` to show "submitted for approval" vs "saved" like the web toast does.
- 2026-09-16 — `types/employee.ts` (new), `types/finance.ts` (`EmployeeSalaryConfig`, `EmployeePayoutBreakdown`, `EmployeePayrollRow`, `CalculateEmployeePayrollResult`, `Payout.staffKind`), `firestore.rules` (new `center_employees` block, mirrors `rooms`), `services/employeeService.ts` (new), `hooks/useCenterEmployees.ts` (new), `services/financeService.ts` (`calculateEmployeePayrollApi`, `saveEmployeePayoutApi`, `saveEmployeeSalaryApi`), `lib/server/financeOps.ts` (`updateEmployeeSalary`, `calculateEmployeePayroll`, `saveEmployeePayout`), `app/api/manager/finance/employee-salary/route.ts` (new), `app/api/manager/finance/payroll/employees/{calculate,save}/route.ts` (new), `app/manager/employees/*` (new), `app/manager/finance/_components/EmployeePayrollTab.tsx` (new), `app/manager/finance/page.tsx`, `app/manager/staff-attendance/page.tsx`, `app/manager/layout.tsx`, `docs/EMPLOYEES.md` (new): **new "Xodimlar" (Employees) nav item — non-teaching staff (receptionist, cleaner, security, driver, HR…), the Android manager app must mirror the whole roster+payroll surface.** ⚠️ **New collection `center_employees/{autoId}`** — critically, this is **NOT** a login-account collection like `center_teachers`/`center_staff`: there is no Auth user, no username, no credentials doc, and the doc id is a plain Firestore auto-id (not a uid). Rules mirror the `rooms` collection exactly: `create`/`update` just require `isActiveCenterManager(centerId)` — a **plain client write**, no API route for basic CRUD (`services/employeeService.ts`'s `createEmployee`/`updateEmployee`/`deleteEmployee` are direct `addDoc`/`updateDoc`/`deleteDoc` calls). Shape: `{centerId, employeeName, role: 'receptionist'|'cleaner'|'security'|'driver'|'hr'|'other', phone?, createdBy, addedAt, salary?: {fixed?, hourlyRate?, allowances?: {label,amount}[], deductions?: {label,amount}[]}}` — `salary` is the ONE field NOT client-writable (API-only, `POST /api/manager/finance/employee-salary`, same posture as `center_teachers.salary`). ⚠️ **The employee's `center_employees` doc id doubles as its identifier in TWO existing collections**: `center_staff_attendance/{staffUid}_{date}` (staffUid = the employee doc id — `app/manager/staff-attendance/page.tsx` now merges teachers AND employees into ONE roster fed to the same grid, no new attendance shape) and `center_payouts/{teacherId}_{YYYY-MM}` (teacherId = the employee doc id, with a new optional `staffKind: 'employee'` field — `markPayoutPaid` is REUSED VERBATIM, unchanged, for both staff kinds). New routes, mirroring the teacher payroll routes' exact guard shapes: `POST /api/manager/finance/employee-salary` (manager-only), `POST /api/manager/finance/payroll/employees/calculate` (`office: 'any'` — read-only), `POST /api/manager/finance/payroll/employees/save` (manager-only). `POST /api/manager/finance/payroll/mark-paid` is the EXISTING route, unchanged, reused for employee payouts too. No new index (single-field `centerId ==` equality only). Full contract: `docs/EMPLOYEES.md`.
- 2026-09-16 — `lib/finance/financeStats.ts` (new), `lib/finance/billingEngine.ts` (`shiftMonthKey` moved here), `app/manager/_components/monthLabels.ts` (new), `app/manager/finance/_components/financeFormat.ts` (re-exports, no behavior change), `app/manager/finance/page.tsx`, `app/office/page.tsx`, `app/manager/dashboard/page.tsx`, `app/manager/dashboard/_components/{RevenueExpenseTrendChart,ExpenseCategoryPieChart,AttendanceTrendChart}.tsx` (new), `docs/FINANCE.md` (§12): **the manager dashboard now shows finance charts, not just attendance — no Firestore shape or API change, purely a new read/render pattern the Android manager app can mirror if/when it wants parity.** No new collections, no new routes, no rules change — everything here reads EXISTING data through EXISTING client-side fetchers (`fetchChargesForMonth`, `fetchPaymentsForMonth`, `fetchOpenCharges`, `fetchExpensesForMonth`, `fetchCenterSessions`), just called for more months and reduced through a newly-shared `computeFinanceStats`/`groupExpensesByCategory` (extracted from what `finance/page.tsx` and `office/page.tsx` were each already computing independently — pure dedup, not new logic). If Android adds this: (1) the dashboard's income/debt KPI cards now come from the exact same stat formula the Moliya page and the office panel use — `collected` = confirmed month payments (payment − refund), `charged` = non-cancelled month charges, `debtTotal` = all-time open charge remainders, `expensesTotal` = **exact-string** `status === 'active'` month expenses (excludes the `pending_approval`/`rejected` statuses from 2026-09-16's expense-approval feature), `profit` = collected − expensesTotal; (2) a 6-month revenue-vs-expense trend is built by calling the payments/expenses fetchers once per trailing month key; (3) a 6-month attendance-rate trend buckets one wider `center_attendance` range query by month and reduces each bucket with the existing rate formula (docs/ATTENDANCE.md rule #6); (4) the expense-category pie groups the current month's active expenses by `category`, identical to the existing Xarajatlar tab's category chips. No dependency change (`recharts` was already a dependency for the student/teacher charts).
- 2026-09-16 — `types/branch.ts` (new), `firestore.rules` (new `center_oversight` block), `app/api/manager/switch-branch/route.ts` (new), `app/api/admin/centers/route.ts` (new `attachToOwnerUid` mode), `services/branchService.ts` (new), `app/manager/branches/page.tsx` (new), `app/manager/layout.tsx` (conditional "Filiallar" nav item), `app/admin/centers/[id]/_components/BranchesTab.tsx` (new), `app/admin/centers/[id]/page.tsx` (new tab): **multi-branch owner view — one owner can now personally operate several `centers` docs, switching which one is active.** ⚠️ **`centers.ownerUid` is UNCHANGED in meaning and can now legitimately repeat across several `centers` docs for the same owner uid** — every existing `isCenterManager`/`requireActiveCenterManager` check and every finance/attendance/roster rule needed ZERO changes; Android's existing single-center assumption per session still holds, since the active branch is still just `users.centerId`, a single scalar. New collection **`center_oversight/{ownerUid}_{centerId}`**: `{ownerUid, centerId, centerName?, accessLevel:'operate', addedAt, addedBy}` — an explicit grant record, `write: if false` for every client (super-admin-only, via the extended `POST /api/admin/centers`), `list` a plain `ownerUid==` field compare (no `get()` cost). **New route `POST /api/manager/switch-branch {centerId}`** — the ONLY place `users/{uid}.centerId` is ever rewritten after signup (that field is locked against client update); verifies the caller directly owns the target center OR holds a `center_oversight` grant for it, requires the target `status:'active'`, then updates `users.centerId`. ⚠️ **If the Android manager app ever adds branch-switching, it must force a FULL app-level re-fetch/restart of every centerId-scoped screen after calling this route** — this repo's own web client learned the hard way that a soft navigation leaves stale-branch data on screen (the manager layout only resolves `centerId` once per mount). `POST /api/admin/centers` gained an optional `attachToOwnerUid` field (super-admin only, not manager-facing) that skips Auth-user creation and attaches a new branch to an existing manager instead — irrelevant to the Android manager app unless it ever gets an admin surface. No index changes (single-field equality only).
