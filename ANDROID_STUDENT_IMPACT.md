# Android STUDENT impact ledger

One line per change that the Android **student** app (`/home/matimatik/AndroidStudioProjects/EdifyStudent`)
must mirror. Consumed (and pruned back to this header) by that repo's `/sync-web` command.

Format: `- YYYY-MM-DD | <files> | <what the Android student app must mirror>`

<!-- Baseline: the Android student app was ported 1:1 from this repo's working tree
     of 2026-07-20 (every app/(student) page incl. the raschmodel suite, lib/xp,
     questionSchema, Exam*/RASCH* libs, checkers). Synced through the commit that
     introduces this file. -->
<!-- synced through 986f880 by EdifyStudent sync (2026-07-21) -->
- 2026-07-29 · `lib/RASCHquiz.ts` (`testTypeFor`/`teacherQuizItem`), `docs/RASCH_QUIZ.md` — an open **ko'p savolli** block (`multi_part` with typed parts) is now filed as test type **`O`** instead of `Y-1`/`Y-2`; blocks are judged by `isClosedQuestion` (a `shared_options` pool, or per-part option lists, stays closed). Mirror it wherever the Android app derives a test type for a teacher-picked item; stored `teacher_rasch_quizzes.items[].testType`/`sectionId` change for NEW papers only (existing papers keep their snapshot), and rendering is unaffected — `examMode` returns `'block'` before reading `testType`.
- 2026-07-29 · `data/rasch-default-paper.json` (new), `lib/RASCHdefaultPaper.ts` (new), `app/teacher/create/rasch/build/page.tsx`, `docs/RASCH_QUIZ.md` — a teacher can now fill a paper from a bundled 45-question **default variant** ("Namunaviy variant"). Teacher-side only, but the items it writes into `teacher_rasch_quizzes.questions[]` are `source: 'teacher'` snapshots with ids `rasch-default-NN` and `qType` `mcq`/`numeric`/`open` — the student runner must keep rendering them off `examMode` (typed input for the ten O items) with no lookup by id, since no `teacher_questions` document backs them.
- 2026-07-29 · `lib/RASCHmarks.ts`, `app/teacher/create/rasch/results/[quizId]/page.tsx`, `docs/RASCH_QUIZ.md` — dynamic marks are no longer a per-section budget: every question is now priced across the WHOLE paper (`weight = exp(0.35·difficulty logit)`, ball clamped to `BALL_MIN 1.3`…`BALL_MAX 6.0` via a water-filling solve, total exactly `PAPER_TOTAL 100`); `SlotMark.sectionBudget` → `sectionTotal` (reported after the split, not reserved), `LOGIT_SLOPE`/`MULT_MIN`/`MULT_MAX` → `BALL_SLOPE`. Reads only `teacher_rasch_results.items` — no document shape change — but any Android port of RASCHmarks must mirror the new allocator or the two clients will show different balls for the same cohort.
- 2026-07-29 · `lib/RASCHmarks.ts`, `lib/RASCHscale.ts`, `app/teacher/create/rasch/results/[quizId]/page.tsx`, `docs/RASCH_QUIZ.md` — question balls are now an ABSOLUTE continuous function of the cohort solve rate (`ball = 1.3 + 4.7·(1−p̂)²`, `BALL_MIN 1.30`…`BALL_MAX 6.00`, two decimals) instead of a whole-paper allocation summing to 100; balls no longer total 100 — the student's score is normalised instead (`normalizedScore = Σ earned / Σ all × 100`), so solving hard items now scores far above solving the same number of easy ones. `shareOut`/`itemWeight`/`BALL_SLOPE` removed, `ballFor`/`BALL_SPAN`/`BALL_CURVE` added. New `evidenceConfidence`/`shrinkLevel` in RASCHscale (1−e^(−n/6)) rank a student's strongest/weakest dimension without double-shrinking the EAP level. No Firestore shape change (still reads `teacher_rasch_results.items`); θ and the 0–5 levels are untouched.
- 2026-07-29 · `lib/RASCHmarks.ts`, `app/teacher/create/rasch/results/[quizId]/page.tsx`, `docs/RASCH_QUIZ.md` — `MIN_RESPONSES` is no longer a gate that froze the whole paper on the DTM printed balls; every question now shows its CURRENT ball from the first response (only a never-answered slot falls back to `printedBall`), `SlotMark.provisional` became a per-slot "still moving" label, and balls render with two decimals. No Firestore shape change.
- 2026-07-29 · `app/teacher/create/rasch/results/[quizId]/page.tsx`, `docs/RASCH_QUIZ.md` — the results headline (student row + average tile) now leads with the BALLS earned out of the paper total (`65.56/112.20 · 58.4%`) instead of the normalised percentage alone, so it matches the per-question grid; totals render with two decimals. Display only — no logic or Firestore change.
- 2026-07-29 · `services/teacherRaschQuizService.ts` (`listMyQuizResults`), `app/(student)/raschmodel/quiz/page.tsx`, `tests/rules/raschquiz.rules.test.mjs`, `docs/RASCH_QUIZ.md` — the student's `/raschmodel/quiz` code screen now lists every teacher paper they have sat (title, correct/incorrect counts, `percent`, 0–5 level, date) from a NEW query `teacher_rasch_results where studentId == uid` — equality-only, no composite index, and the `studentId ==` clause is what makes it provable against the existing read rule (no rules change; new test pins it). Cached 60s at module level and PATCHED on submit. ⚠️ The score shown is `percent`, not the teacher's dynamic ball — a ball needs the whole cohort, which a student may not read.
- 2026-07-29 · `app/(student)/raschmodel/_components/ExamReview.tsx` (new optional `outcomes` prop), `app/(student)/raschmodel/quiz/page.tsx`, `docs/RASCH_QUIZ.md` — each row of the student's sat-papers list now expands into the SAME `ExamReview` (grid + dialog) in **replay mode**: correctness comes from `teacher_rasch_results.items` keyed by `examSlotKeys` (`id#partId` — NOT the grid's `id:partId` React key) instead of live answers, there is no "your answer" line (outcomes are not answers), a `multi_part` block's per-part ticks are omitted (one slot, one outcome), and `hideAnswers` still follows the paper's `showAnswers`. Costs one `teacher_rasch_quizzes` read per paper, cached for the tab; offered only when `items` exists. No Firestore shape change.
- 2026-07-29 · `lib/RASCHmarks.ts` (`capBalls`), `app/teacher/create/rasch/results/[quizId]/page.tsx`, `docs/RASCH_QUIZ.md` — a paper is now CAPPED at `PAPER_TOTAL` 100: when the absolute per-question curve would total more, every ball is scaled down by one common factor (largest-remainder rounding to 2dp so the sum is exactly 100.00); a paper under 100 is left alone. Consequence to mirror: a displayed ball can fall BELOW `BALL_MIN` 1.30 (a full-spread 45-question paper renders 1.05…4.31) — the cap preserves order and ratios, not the floor. `paperTotal` is therefore ≤ 100, and the results headline drops the now-redundant percentage when the total is exactly 100.
- 2026-07-29 · `app/(student)/raschmodel/components/MmsCard.tsx` (new), `app/(student)/dashboard/page.tsx`, `docs/STUDENT.md` — new **MMS (Matematika Milliy Sertifikat)** hero card on the student dashboard, directly under `MathLevelSummary`: names the mock exam, shows `EXAM_TOTAL_QUESTIONS`/`EXAM_DURATION_MINUTES`/`PAPER_TOTAL` (read from the libs, never hardcoded), the student's `expectedScore(θ)` and their weakest MEASURED dimension linked to `/raschmodel/topic/{key}`, plus CTAs to `/raschmodel/exam` and `/raschmodel/quiz`. Reads only the existing 12h-cached `RASCH_levels/{uid}` document — no new query, no Firestore shape change.
- 2026-07-30 — `app/(student)/milliy-sertifikat/page.tsx`, `app/(student)/layout.tsx`, `lib/MilliyQuiz.ts`, `types/MilliyQuiz.ts`, `services/milliyQuizService.ts`, `lib/Examsession.ts`, `types/Exam.ts`, `data/question_topics.json`, `firestore.rules` (§12c), `firestore.indexes.json`: **NEW Milliy sertifikat student page** — one 6-digit code box for every subject. `findPaperByCode` queries **both** `milliy_quizzes` (new collection: biology subject papers) and `teacher_rasch_quizzes` (maths); a maths code must be handed to the Rasch runner (it alone moves `RASCH_levels`), a subject paper is sat in the shared `ExamRunner` and scored on **raw counts only** — no theta, no 0-5 level. New result collection `milliy_quiz_results/{quizId}_{uid}` (deterministic id, retake overwrites, delete denied): `{quizId, quizTitle, subject, teacherId, studentId, studentName, correct, total, percent, durationSec, submittedAt (epoch ms), examLang, items?: Record<slotKey,0|1> keyed by examSlotKeys, topics?: Record<topicSlug,{correct,total}>}` — ONE write on submit. New localStorage store key `milliy:quiz:v1` (a THIRD store — must not share with `raschmodel:exam:v1`/`raschmodel:quiz:v1`). `ExamSnapshot` gained optional `milliySubject` (set ⇒ the snapshot is a subject paper). `data/question_topics.json` gained subject `biologiya` (8 topics / 126 subtopics) — mirror the file verbatim; ids are persisted into question docs. See docs/MILLIY_QUIZ.md.
- 2026-07-30 — `hooks/useExamLockdown.ts` (new), `app/(student)/raschmodel/_components/ExamRunner.tsx`, `app/(student)/raschmodel/exam/page.tsx`, `app/(student)/raschmodel/quiz/page.tsx`, `app/(student)/milliy-sertifikat/page.tsx`, `docs/STUDENT.md`: **exam lockdown** — the shared runner is now a full-screen overlay covering the app nav for ALL three sittings (mock exam, teacher maths paper, subject paper), and it counts + warns on every interruption (tab/app switch, leaving full screen, reload attempt). Android equivalent: immersive/fullscreen mode with the nav host hidden, `onPause`/`onWindowFocusChanged` as the interruption signal, a non-dismissable dialog (no back-button dismiss) and a visible exit counter. **No Firestore shape change** — the count is view-only (the hook exposes an optional `onInterruption` callback if it is ever persisted). Full screen must be released on submit.
- 2026-07-30 — `app/(student)/layout.tsx`, `app/(student)/milliy-sertifikat/page.tsx` (now a subject hub), `app/(student)/milliy-sertifikat/[subject]/page.tsx` (new; the old hub's code box + sitting moved here), `lib/MilliyQuiz.ts` (`studentSubjectHref`), `lib/RASCHmarks.ts`, `lib/MilliyDefaultPaper.ts` (new), `data/biology-default-paper.json` (new): **IA change + shared marking.** (1) The student nav now has ONE entry, `Milliy sertifikat` — the `/raschmodel` item is GONE; the hub's Matematika card links to `/raschmodel` instead. Mirror this: one drawer/nav destination, subjects inside, maths opening the existing Rasch section. (2) Per-subject student route `/milliy-sertifikat/{subject}` holds the code box, sitting and that subject's sat papers; the hub is cards only and reads nothing. (3) **Marking is now identical on every subject**: a question's worth is `1.3 + 4.7*(1-p_hat)^2` from the cohort solve rate, paper capped at 100, score = sum(earned)/sum(all)*100 (`lib/RASCHmarks.ts`, unchanged arithmetic; `paperMarks` gained an injectable `printed` argument and subject papers pass `evenPrinted`). Students still see PERCENT on their own result — a ball needs the whole cohort, which a student may not read. **No Firestore shape change.** See docs/MILLIY_QUIZ.md.
- 2026-07-30 — `lib/MilliyQuiz.ts` (`MILLIY_SUBJECTS.kimyo`: `soon` → `generic`), `data/question_topics.json` (new subject `kimyo`), `data/chemistry-default-paper.json` (new), `lib/MilliyDefaultPaper.ts` (`SAMPLE_FILES.kimyo`), `scripts/addChemistryTopics.mjs` (new), `docs/MILLIY_QUIZ.md`, `docs/QUESTIONS.md`, `docs/TEACHER.md`, `docs/DATA_MODEL.md`: **Kimyo (chemistry) is now a live Milliy sertifikat subject.** Student-visible: the `/milliy-sertifikat` hub card for Kimyo stops being *soon* and opens `/milliy-sertifikat/kimyo`, which sits a chemistry paper by 6-digit code through the SAME runner, the same `milliy:quiz:v1` store and the same raw-count scoring as biology — mirror it by making the subject list data-driven off the registry rather than adding a chemistry screen. **NO Firestore shape change, no new collection, no new rule, no new index** (`milliy_quizzes.subject` already accepted `'kimyo'`, and the picker reuses `teacher_questions creatorId + subject.id + createdAt desc`). `data/question_topics.json` gained subject `kimyo` — **4 topics / 117 subtopics**; mirror the file verbatim, ids are persisted into question docs. ⚠️ Chemistry's topic level is only 4 entries because it mirrors the programme's four numbered sections (I. Umumiy / II. Anorganik / III. Organik / IV. Kimyoviy tahlil); the intermediate groups are folded into subtopic NAMES as `"Group — Leaf"`. ⚠️ The builder defaults to **40 questions / 100 minutes, not 43 / 180**: the programme's #41-43 are extended written work needing human per-step marking, which no client can do. See docs/MILLIY_QUIZ.md.
- 2026-07-30 — `app/(student)/dashboard/_components/MilliyCard.tsx` (new), `app/(student)/dashboard/page.tsx`, `app/(student)/raschmodel/components/MmsCard.tsx` (comment only), `docs/STUDENT.md`, `docs/MILLIY_QUIZ.md`: **the student dashboard's maths-level card is replaced by a Milliy sertifikat card.** `MathLevelSummary` no longer renders on the dashboard (it stays on `/raschmodel`); its slot now holds `MilliyCard` — a link to `/milliy-sertifikat` plus one chip per subject, each going straight to `studentSubjectHref(subject)` (`math` → `/raschmodel`, generic → `/milliy-sertifikat/{id}`, `soon` → inert chip). Reason to mirror: the nav entry is hidden from the 5-slot mobile dock, so on a phone the programme was reachable only from the drawer. `MmsCard` below it is unchanged and remains the dashboard's only 0–5 level readout. **Zero Firestore reads** (the list is `MILLIY_SUBJECTS`), no query, no document-shape change.
- 2026-07-31 — `app/(student)/ielts/page.tsx`, `app/(student)/ielts/_components/{GroupsTab,PracticeTab,ProgressTab}.tsx` + `{hubTexts,hubData}.ts` (all new), `docs/IELTS.md`: **the student IELTS hub is now tabbed** — Guruhlarim / Amaliyot / Yo'lim instead of one long scroll. Mirror as three tabs (or three destinations) on the IELTS screen: groups list (unchanged cards, still NO `joinCode`), the platform Practice Library with the skill filter, and the band-journey view (overall/attempts/graded tiles + sparkline + per-skill latest/best). Web keeps the tab in the query string (`/ielts?tab=groups|practice|progress`; `?skill=`/`?type=` from the group page's weak-type link imply the practice tab, no `?tab=` → practice when the student has no groups, else groups) — the Android equivalent is the same defaulting rule plus deep-link handling, and the selected tab must survive process death. **No Firestore shape, query, rule or API change** — same `ielts_groups` array-contains snapshot and the same one `fetchMyAttempts` call, now shared by the practice best-band chips and the journey tab.
- 2026-07-31 — `lib/ielts/types.ts` (`IeltsCatalogControls`), `services/ieltsService.ts` (`fetchPlatformTestSummaries` gained an `audience` option + `compareCatalog`), `lib/ielts/importValidation.ts` (new), `app/admin/ielts/page.tsx`, `data/ielts practise/*`, `docs/IELTS.md`: **the IELTS platform dataset is now an admin-curated catalog.** `ielts_test_meta/{testId}` and the four `ielts_*_tests` docs gained three **additive, optional** fields — `catalogOrder?: number`, `hiddenFromStudents?: boolean`, `hiddenFromTeachers?: boolean` — written only by `/admin/ielts`. Mirror in the student Practice Library: **drop every summary with `hiddenFromStudents == true`, then sort ascending by `catalogOrder` with missing/non-numeric last (tie-break: title A→Z)** — that is `compareCatalog`. Filter and sort **client-side**: a `whereNotEqualTo("hiddenFromStudents", true)` query would silently drop every legacy doc that has no such field. Absent field = visible. Hiding affects the browsable library ONLY — a test already assigned to a group, and every existing `ielts_attempts` doc, must keep opening normally. No rules change (admin writes go through the `super_admin` god-mode wildcard), no new index, no API change.
- 2026-07-31 — `app/(student)/ielts/_components/runner/highlight.ts` (new), `app/(student)/ielts/_components/runner/IeltsRunner.tsx`, `app/(student)/ielts/_components/runner/i18n.ts`, `docs/IELTS.md`: **reading-runner UX fixes.** (1) The **marker/pen now works**: highlighting was `range.surroundContents()`, which throws on any selection that partially contains an element (every selection crossing `<br>`, `<b>` or a paragraph), and the error was swallowed — replaced by a text-node walker that snapshots each slice BEFORE mutating (the live Range moves its own `endOffset` mid-loop otherwise), refuses to nest marks, and unwraps back to byte-identical HTML. Mouse marks on release; touch/pen get a confirm bubble because `touchend` fires while the selection handles are still live. New eraser button clears the passage. Android equivalent: a Spannable/`BackgroundColorSpan` over the passage TextView with the same rules (per-passage persistence, tap-to-remove, clear-all). (2) **8 font steps** (12→30px) instead of 3, and (3) a **drag-to-resize passage/questions divider** (25–75%, double-click resets); both persist in device-local storage (`ielts:runner:font`, `ielts:runner:split`) — mirror with SharedPreferences, not Firestore. (4) The practice **"Tekshirish" (Check) button is GONE** — submit is the only action; `/api/ielts/check` still exists but nothing calls it, so do NOT add it to the Android runner. **No Firestore shape, query, rule or API contract change**; the runner session store keeps the same `highlights` map (serialized per-block innerHTML on web).
- 2026-07-31 — `app/(student)/ielts/_components/runner/highlight.ts`, `app/(student)/ielts/_components/runner/IeltsRunner.tsx`, `app/(student)/ielts/_components/runner/StudentQuestionRenderer.tsx`, `app/(student)/ielts/_components/runner/i18n.ts`, `docs/IELTS.md`: **reading-runner marker is now buttonless and symmetric** (supersedes the earlier entry today). Select passage text → it turns yellow (`gold-container`); select over yellow → the mark is removed; there is NO marker toggle, NO eraser button and NO click-to-remove (a stray click while reading would erase silently). Removal is decided by strict range overlap (`compareBoundaryPoints`), not `intersectsNode`, so marking the word next to a mark keeps the mark. **Right-click / context menu is disabled for the whole sitting in BOTH modes** (was simulation-only) — Android equivalent: block the long-press text-selection ActionMode/context menu and drive marking from the selection itself. Also: passage body dropped `font-serif` and question body rows went `0.95em`/`md:1em` → `1em` so **passage and question text render at exactly the same size** at every one of the 8 font steps (both panes share one `fontSize` + `lineHeight: 1.65`). **No Firestore shape, query, rule or API change**; the session store still carries the same `highlights` map.
- 2026-07-31 — `app/(student)/ielts/_components/runner/highlight.ts`, `app/(student)/ielts/_components/runner/IeltsRunner.tsx`, `docs/IELTS.md`: **two selection bugs found with a real-browser (CDP) repro of the reading runner.** (1) The marker erased on ANY overlap with an existing mark, so selecting a phrase that merely starts inside a mark deleted it and highlighted nothing, and **triple-click marked nothing** (the double-click inside it marks a word, the paragraph selection then wiped it). New rule, mirror it exactly: erase only when every non-whitespace part of the selection is already marked, otherwise mark the unmarked parts (extend); whitespace-only parts are ignored in that test so re-selecting a marked word still erases. (2) `tabSwitches` anti-cheat counted a bare `window.blur` — on Android the text-selection toolbar and the soft keyboard blur the window while the page still has focus, so **selecting text or typing an answer registered as a tab switch** (and popped the simulation blocker) in BOTH reading and listening; it now ignores `blur` while `document.hasFocus()` is true, with `visibilitychange` as the primary signal. Android must apply the same guard to whatever it uses for its leave-the-app counter — a selection ActionMode or IME must not increment it. **No Firestore shape, query, rule or API change.**
- 2026-08-01 — `lib/RASCHmarks.ts`, `docs/RASCH_QUIZ.md`, `docs/MILLIY_QUIZ.md`: **the dynamic marking formula changed** — a question is now `B = Y + σ` (guaranteed base by test type: `BASE_BALL` Y-1 1.1 / Y-2 1.3 / O 1.6, plus a share of the difficulty budget `σ = (100 − ΣY)·w/Σw` with `w = (1 − p̂)²`), summing to exactly 100 with no cap. `BALL_MIN`/`BALL_MAX`/`BALL_SPAN`/`ballFor`/`capBalls` are gone; `BASE_BALL`/`BASE_SHARE`/`baseBalls`/`difficultyWeight`/`SlotMark.base` are new. ⚠️ **Student-side impact is limited to the shared lib**: the student still sees `percent` and the 0–5 level on their own sitting (a ball needs the whole cohort, which a student may not read — rules enforce it), and `PAPER_TOTAL` is still 100, so `MmsCard`'s "45 savol · 150 daqiqa · 100 ball" copy is unchanged. Port the file if the student app mirrors it; **no Firestore shape, query, rule or API contract change**.
- 2026-08-03 — `lib/MilliyQuiz.ts` (`MILLIY_SUBJECTS`), `types/MilliyQuiz.ts` (`MilliySubjectId`), `lib/MilliyDefaultPaper.ts` (`SAMPLE_FILES`), `data/question_topics.json`, `data/physics-default-paper.json` (new), `data/ona-tili-default-paper.json` (new), `scripts/addPhysicsTopics.mjs` (new), `scripts/addNativeLanguageTopics.mjs` (new), `app/(student)/milliy-sertifikat/page.tsx`, `lib/announcements.ts`, `docs/MILLIY_QUIZ.md`: **Milliy sertifikat gained two live subjects — `fizika` and the NEW `ona-tili`** (Fizika flipped from `soon` to `generic`; Ona tili is a brand-new registry entry). ⚠️ **`MilliySubjectId` is now `'biologiya' | 'kimyo' | 'fizika' | 'ona-tili' | 'ingliz'`** — widen the Kotlin enum/serializer, and note `ona-tili` is the **first id containing a hyphen**: it is simultaneously the route segment (`/milliy-sertifikat/ona-tili`), the stored `milliy_quizzes.subject` / `milliy_quiz_results.subject` value and the `question_topics.json` subject slug, so never normalize it to `ona_tili`. An unknown-subject value must not crash an older client. Student-side surfaces are all registry-driven: the hub and the dashboard `MilliyCard` chip row now show five open subjects and only Ingliz tili as *soon*, and two dashboard announcements were added (`mms-fizika-2026`, `mms-ona-tili-2026`) while the hub announcement dropped "Fizika … tez kunda". ⚠️ Ona tili's TYPED answers are graded as **text** and text matching does NOT normalize apostrophes, so any answer key the app ships/echoes must stay apostrophe-free (the sample paper's are). **No Firestore shape, query, rule, index or API contract change** — only the value space of `subject` widened, and the taxonomy JSON gained `fizika` (5 topics / 98 subtopics) and `ona-tili` (7 topics / 84 subtopics).
- 2026-08-03 — `parent_links` (new collection), `app/api/parent/[token]/route.ts` (new), `lib/server/parentReport.ts` (new), `firestore.rules` (§1.6), `docs/PARENTS.md` (new): **parent access by QR — informational for the student app, NO Android student work.** A manager can now issue a QR that lets a parent open a read-only web report about ONE student (`/p/{token}`) with no account at all. It is assembled server-side with the Admin SDK from data the student app already writes — `users` (displayName/photoURL/totalXP/currentStreak), `classes`, `attempts`, `milliy_quiz_results`, `teacher_rasch_results`, `RASCH_levels`, `center_attendance`, `center_student_finance`/`center_charges`/`center_payments` — **read-only, no shape change to any of them**, and `parent_links` is denied to every client (never query it from the app). ⚠️ The one thing to keep in mind when changing student writes: a parent now reads these fields too, so a regression in `attempts.type`/`score`/`totalQuestions`/`submittedAt` or in `milliy_quiz_results.percent` becomes visible to families, not just to the student. ⚠️ An exam attempt is reported as *pending* until `status:'graded'` — do not start writing a partial `teacherScore` before grading.
- 2026-08-03 — `app/api/parent/[token]/route.ts`, `types/Parent.ts`, `docs/PARENTS.md`: **parent links are now one-device-per-child** (amends today's earlier parent entry). ⚠️ Still **no Android student work**: the parent report remains a server-assembled read of data the student app already writes, `parent_links` stays denied to every client, and no student-side collection changed shape. Logged because the ledger tracks every Firestore shape change — `parent_links` gained `deviceHash?`/`claimedAt?`/`claimedDevice?`.
- 2026-09-14 — `hooks/useCenterClasses.ts` (`ClassData.schoolClassId`/`schoolClassName`), `firestore.rules` (new `school_classes` collection), `docs/MANAGER.md`: **informational only, no Android student work.** The manager panel's "Groups" page was redesigned into grade-based "School Classes" (e.g. "5-A") grouping several subject `classes` docs under one shared roster (see `ANDROID_IMPACT.md`). Student-facing join/roster flows are completely unchanged — center groups already blocked student self-join before this, and personal teacher groups/join codes are untouched. The only thing that reaches a `classes` doc a student reads is two new **additive, optional** fields, `schoolClassId`/`schoolClassName`, which the student app can keep ignoring. Logged only because the ledger tracks every Firestore shape change.
- 2026-09-15 — `app/(student)/sat/page.tsx`, `app/(student)/sat/math/page.tsx`, `app/(student)/sat/_components/SatRunner.tsx`, `docs/SAT_QUIZ.md`: **SAT is now English-only.** The student-facing `/sat` hub, code-entry/intro/results screen and `SatRunner` no longer follow `useStudentLanguage()` — all chrome is plain English, and the intro card's uz/ru/en question-language picker is GONE. `app/(student)/sat/math/page.tsx` now hard-sets `examLang: 'en'` on every NEW `sat_math_results` write instead of letting the student choose — mirror by dropping the language picker and always requesting English question content. A question's content stays trilingual in Firestore (`SatQuizItem.question`/`.options` are still `{uz, ru, en}`); only the runner's reading key changed, so **no Firestore shape change**. Sittings from before this change may still carry `examLang: 'uz' | 'ru'` — reviewing them must still render in that original language, not be forced to English. Also cosmetic/web-only (no Android equivalent needed): `SatRunner` gained a two-pane split layout with a draggable divider (question column left, workspace column right showing the question's `imageUrl`), local UI state only, not persisted.
- 2026-09-15 — `app/(student)/sat/page.tsx`, `app/(student)/sat/english/page.tsx` (new), `app/(student)/sat/_components/SatRunner.tsx` (new `subjectName`/`onSave` props, A-/A+ text size), `lib/SatEnglishSession.ts` (new), `services/satEnglishQuizService.ts` (new), `docs/SAT_QUIZ.md`: **SAT English (Reading & Writing) is now sittable** — the hub card at `/sat` is live (was "soon"), leading to `/sat/english`: same code-box → intro → sitting → results flow as `/sat/math`, own localStorage session key `sat:english:v1` (a sitting can never collide with a Math sitting), own results collection `sat_english_results` (SAME shape as `sat_math_results` — mirror it once for both subjects). The sitting UI itself is the EXACT SAME component Math already uses (`SatRunner`) — no new runner to port, no passages/highlighting were needed (every English item is a single self-contained paragraph + question + 4 choices, never a shared long passage). Two things now apply to BOTH subjects, mirror on whichever runner/screen this repo's Android app uses for SAT: (1) a draggable two-pane split (question column left, workspace column right showing the question's image if any; local UI state, not persisted); (2) an **A-/A+ text-size control** (8 steps, 12px→30px, same ramp as the IELTS reading runner), remembered per device in `localStorage` key `sat:runner:font` — shared across Math and English since it's a reading preference, not exam content. No Firestore shape change to anything Math already had.
- 2026-09-15 — `app/(student)/layout.tsx` (role redirect only), `firestore.rules` (new `isCenterOffice()` helper, new `center_staff` + `center_staff_credentials` blocks, widened READ rules on the finance collections), `services/userService.ts` (`UserProfile.role` union), `docs/OFFICE.md` (new): **informational only, no Android student work.** Two new center back-office roles (director / buxgalter) got their own web panel at `/office` — see `ANDROID_IMPACT.md` for the real contract. The only student-tree change is one more branch in the layout's role guard (`director`/`accountant` → `/office` instead of falling through to the student dashboard); mirror it only if the Android student app has an equivalent role-based landing switch, and note that `users.role` is a **redirect hint, never an authorization source** (the real gate is the Admin-SDK-only `center_staff/{uid}` link doc). **Nothing a student reads or writes changed**: no student-visible collection, query, index or API contract was touched, and `write: if false` still holds on every finance collection. Logged only because the ledger tracks every `firestore.rules` change.
- 2026-09-16 — `lib/SATscore.ts` (rewritten: `SAT_RAW_SCORE_TABLE`, `estimateScoreBand`, `formatScoreBand`; `estimateScaledScore` kept but now returns the band midpoint), `types/SatQuiz.ts` (`SatMathResult.scoreBand?`, `SatMathResult.omitted?`), `app/(student)/sat/_components/SatRunner.tsx`, `app/(student)/sat/_components/SatScoreSummary.tsx` (new), `app/(student)/sat/math/page.tsx`, `app/(student)/sat/english/page.tsx`, `docs/SAT_QUIZ.md`: **SAT scoring moved from a straight line to the real published conversion CURVE, and the score report now distinguishes BLANK from WRONG.** ⚠️ **Mirror the scoring change or Android and web will report different scores for the same sitting.** `estimateScaledScore` was `200 + (ceil-200) * (correct/total)` rounded to 10; it is now the midpoint of `estimateScoreBand`, which reads a 55-row raw→scaled band table (raw 0…54, from the published digital SAT Practice Test 11 conversion table) — copy `SAT_RAW_SCORE_TABLE` verbatim from `lib/SATscore.ts`. ⚠️ **THE LENGTH TRAP**: the table is indexed by a 54-question section's raw score, but our tests are teacher-built and any length, so NEVER index it with a raw count — map `correct/total` onto the table's 0…54 domain first, then linearly blend the two neighbouring rows (`bandForFraction`). Indexing a 12/12 sitting as raw 12 reads off [330,370] and reports a perfect paper as failing. The route ceiling is UNCHANGED and still applied on top (easier route clamps both ends to 590, so a perfect easier-route paper is a single 590, not a band). **Two additive, optional result fields to mirror**: `scoreBand: {lower, upper}` (the range, displayed as "640–700"; absent on older sittings → fall back to the single `scaledScore`) and `omitted: string[]` (ids left blank — a blank still scores 0, this is REPORTING only; absent on older sittings → HIDE the right/wrong/blank tally entirely, never render "0 unanswered"). `items` is unchanged and still binary 0/1, which is exactly why `omitted` had to be separate. ⚠️ **Scores written before 2026-09-16 are not comparable to scores after it** and nothing backfills them — expect mixed averages on old tests. Grading itself (`isSatItemCorrect`) was NOT touched. New shared UI `SatScoreSummary` renders band + disclaimer + raw/% + route badge + right/wrong/blank tally + per-module split for both subjects; the past-sittings rows now show the band where present.
- 2026-09-16 — `types/mistakes.ts` (new), `lib/mistakes.ts` (new), `services/mistakeService.ts` (new), `app/(student)/mistakes/page.tsx` (new), `app/(student)/layout.tsx` (nav entry), `app/(student)/sat/{math,english}/page.tsx`, `app/(student)/milliy-sertifikat/[subject]/page.tsx`, `app/(student)/raschmodel/quiz/page.tsx`, `firestore.rules` (new `student_mistakes` block), `firestore.indexes.json` (2 new indexes), `tests/rules/studentMistakes.rules.test.mjs` (new), `docs/MISTAKES.md` (new), `docs/{STUDENT,SAT_QUIZ,DATA_MODEL,README}.md`, `CLAUDE.md`: **"MY MISTAKES" — a new student page at `/mistakes` banking every question answered WRONG or LEFT BLANK, with a "practise 5 similar questions" lookup per row.**
  ⚠️ **New collection `student_mistakes/{studentId}_{slotKey}`** — mirror the shape from `types/mistakes.ts` verbatim: `{id, studentId, slotKey, questionId, source ('sat-math'|'sat-english'|'milliy'|'rasch'), outcome ('wrong'|'blank'), testId, testTitle, question, stem?, imageUrl?, optionKeys[], options{}, answer, explanation?, examLang, subjectId, subjectName, topicId, topicName, subtopicId, subtopicName, difficultyId, timesWrong, lastMissedAt (epoch ms), resolved, resolvedAt?}`. The question is a full SNAPSHOT, not a reference (the paper may be edited/deleted, and the bucket must render from one query). **Rules make it private to that student — NOT readable by their teacher or centre manager**; both branches compare the `studentId` FIELD, never the doc id (a uid is not separable from a slotKey by string surgery — question ids contain `_` too), and `create` additionally pins `mistakeId == request.resource.data.id`. **New index** `student_mistakes: studentId ASC, resolved ASC, lastMissedAt DESC`.
  ⚠️ **Idempotency contract to mirror exactly**: the doc id is `${studentId}_${slotKey}` and the write is `setDoc(..., {merge:true})` with `timesWrong: increment(1)`, `resolved: false`, `resolvedAt: null`. Missing the same slot in a retake UPDATES the row (count up, re-opened, snapshot refreshed) — it must never create a second document. `increment(1)` on a merge into a non-existent doc creates the field at 1, which is what avoids a read-then-write.
  ⚠️ **Capture runs AFTER the result write and is never awaited** (`recordMistakes(...).catch(...)` at all four call sites) — banking a study aid must never cost a student their score. The outcome map is **passed in, never recomputed**, so the bucket can never disagree with the saved score.
  ⚠️ **Two extractors, because the sources disagree on keys** (`lib/mistakes.ts`): SAT's `items` is keyed by plain question id and `omitted` names the blanks (a pre-2026-09-16 sitting has no `omitted`, so every miss records as `wrong`); Milliy/Rasch key by `examSlotKeys` (`id` or `id#partId`) and record no blanks at all, so "was it answered" is recomputed from the ANSWERS map — which uses a DIFFERENT key space, `partKey` (`id::partId`). Reading the answers map with a slot key silently reports every block part as blank; `slotAnswered()` is the only place that conversion may happen. Granularity is per SLOT, so a `shared_options` sub-question is its own row showing the PART prompt with the block statement as `stem`; a `multi_part` block is one row and counts as answered if ANY part was attempted.
  ⚠️ **The 5 practice questions are SIMILAR, not clones.** There is no variant-family concept and question ids are random (`tq_` + auto-id), so no id arithmetic finds "versions" of a question. The query is equality on `subject.id` + `topic.id` ordered by `rand` (wrapping `>=` then `<`, the `lib/Examquestions.ts` pattern), with difficulty and subtopic used only to RANK the fetched pool — making either an equality leg would need more indexes and would return nothing on a narrow cell. **New index** `teacher_questions: subject.id ASC, topic.id ASC, rand ASC`. A mistake from the legacy `questions1` bank gets NO practice set (its numeric zero-padded taxonomy matches nothing in `data/question_topics.json`) and says so — `canPractise()` is the gate. Loaded only on explicit button press, never on mount.
  ⚠️ The rules test suite `tests/rules/studentMistakes.rules.test.mjs` **has not been run** (no Java on the dev machine → no emulator) — run `npm run test:rules` before trusting the deployed ruleset. Class assignments/exams (`attempts`) are deliberately NOT wired in yet.
