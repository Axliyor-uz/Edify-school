# Android TEACHER impact ledger

One line per change that the Android **teacher** app (`/home/matimatik/AndroidStudioProjects/EdifyTeacher`)
must mirror. Consumed (and pruned back to this header) by that repo's `/sync-web` command.

Format: `- YYYY-MM-DD · <files> — <what the Android teacher app must mirror>`

What belongs here: anything touching `app/teacher`, a Firestore document
shape/query/rule the teacher panel reads or writes, or a contract under
`app/api/teacher|ielts|auth|account|directory|analyze|tts` and the
`app/teacher/create/**/api` route handlers. Shared `lib/`/`services/` changes
count too — the teacher panel imports the same question schema, Rasch maths and
IELTS libraries the student runner does.

⚠️ Keep Firestore shapes **additive-only**: APK rollouts lag web deploys by days,
so an old teacher client keeps writing the old shape. If a change is genuinely
breaking, say so in the line — the Android side then raises
`appConfig/androidTeacher.minRequiredVersionCode` after the next Play release.

<!-- Baseline: the Android teacher app was ported 1:1 from this repo at commit
     ae18a08 ("publish", 2026-07-30), working tree clean. Every app/teacher route
     is native. -->
<!-- synced through ae18a08 by EdifyTeacher sync (2026-07-31) -->
- 2026-07-31 — `lib/ielts/types.ts` (`IeltsCatalogControls`), `services/ieltsService.ts` (`fetchPlatformTests(skill, {audience})`, `compareCatalog`, `carryCatalog`, `setPlatformTestVisibility`, `savePlatformTestOrder`), `lib/ielts/importValidation.ts` (new), `app/teacher/ielts/{reading,listening}/page.tsx`, `app/teacher/ielts/groups/[groupId]/_components/AssignTestModal.tsx`, `app/admin/ielts/page.tsx`, `data/ielts practise/*`, `docs/IELTS.md`: **the IELTS platform dataset is now an admin-curated catalog.** The four `ielts_*_tests` docs and `ielts_test_meta/{testId}` gained three **additive, optional** fields — `catalogOrder?: number`, `hiddenFromStudents?: boolean`, `hiddenFromTeachers?: boolean` — written only by `/admin/ielts`. Mirror everywhere the teacher app lists PLATFORM tests (the Reading/Listening "Platform" tab and the assign-test picker): **drop docs with `hiddenFromTeachers == true`, then sort ascending by `catalogOrder`, missing/non-numeric last, tie-break title A→Z** (`compareCatalog`) — and do **not** re-sort platform tests by `createdAt` as the web pages used to. Filter/sort client-side: a `whereNotEqualTo` query drops legacy docs that lack the field. Absent = visible. ⚠️ If the app ever saves a platform test, it must carry these three fields across from the existing doc (`carryCatalog`) — a re-save that omits them wipes the admin's order. The teacher's OWN tests are unaffected. No rules change, no new index, no API change.
- 2026-08-01 — `lib/RASCHmarks.ts`, `app/teacher/milliy-sertifikat/math/results/[quizId]/page.tsx`, `app/teacher/milliy-sertifikat/[subject]/results/[quizId]/page.tsx`, `docs/RASCH_QUIZ.md`, `docs/MILLIY_QUIZ.md`: **the dynamic marking formula changed — port it verbatim.** A question is now `B = Y + σ`: a guaranteed base by test type (`BASE_BALL` = Y-1 1.1, Y-2 1.3, O 1.6) plus a share of the paper's difficulty budget `σ = (100 − ΣY)·w/Σw`, where `w = (1 − p̂)²` and `p̂ = (correct + 1.5)/(seen + 3)` (unchanged shrinkage). On the 32/3/10 DTM paper: bases 55.1, budget 44.9, **the paper totals exactly 100, with no cap**. REMOVED: `BALL_MIN`, `BALL_MAX`, `BALL_SPAN`, `ballFor`, `capBalls` (the old absolute `1.3 + 4.7·d²` curve + 100-point scaling) — delete their Kotlin equivalents. ADDED: `BASE_BALL`, `BASE_SHARE` (0.551, only rescues a paper long enough that its bases alone would reach it), `baseBalls`, `difficultyWeight`, and `SlotMark.base`. A slot nobody has answered is now priced from the prior (w = 0.25) instead of falling back to its printed value, so an unsat paper reads Y-1 2.10 / Y-2 2.30 / O 2.60; `printedBall` is display-only. `paperTotal` is always 100, so `normalizedScore` == `scoreFor` and the teacher UI no longer prints the redundant percentage. **No Firestore shape, query, rule, index or API change** — the marks are still derived on the client from `teacher_rasch_results.items` / `milliy_quiz_results.items` and nothing is stored.
- 2026-08-01 — `app/teacher/milliy-sertifikat/math/results/[quizId]/page.tsx`, `docs/RASCH_QUIZ.md`: **the per-question marks grid on the maths results page is now DENSE** — up to 12 cells a row (`grid-cols-6 sm:grid-cols-9 lg:grid-cols-12`) instead of 6 wide cards, each cell three tiny lines (slot number 8.5px · ball 12.5px · solve rate 8.5px), so all 45 questions fit without scrolling the card. The inline struck-through printed value is GONE: a mark that moved off `printedBall` now carries a dotted underline, and the tooltip carries `#n · ball = base + bonus · printed x.x · pct% · correct/seen · (provisional warning)`. New TR key `printed` (uz *protokol bali* / ru *балл протокола* / en *printed*). The per-student expanded grid matches the same 12-up density. **UI only — no data, query, rule or API change.**
- 2026-08-01 — `app/teacher/milliy-sertifikat/[subject]/results/[quizId]/page.tsx`, `docs/MILLIY_QUIZ.md`: **the subject (biology/chemistry…) results page now renders the marks exactly like the maths one.** The marking itself was already shared (`lib/RASCHmarks.ts`, `B = Y + σ`) — this is the reporting surface: the 40-row `slot · topic · rate · ball` list is replaced by (1) a **topic-grouped** grid — free, because `slotMeta` stores the question's topic slug as `sectionId`, so `SlotMark.sectionTotal` is a per-topic total here — each group headed by what that topic currently adds up to; (2) a **dense 12-up grid** (`grid-cols-6 sm:grid-cols-9 lg:grid-cols-12`) of number · ball · solve-rate cells, coloured by solve rate (<34 error / <67 neutral / else success), dashed under `MIN_RESPONSES`, with `base + bonus`, the closed/typed label and `correct/seen` in the tooltip; (3) the paper's own **`base + bonus = 100.00` split line** — ⚠️ summed per paper, NOT the maths 55.10 + 44.90, since a subject paper's mix of closed (1.1) and typed (1.6) items is the teacher's; (4) the per-student expanded grid at the same density. ⚠️ **No struck-through/underlined "before"** here — these papers have no printed protocol. New TR keys `marksSplit`, `topicTotal`; `slotStats`/`markByKey`/`examSlotKeys` dropped in favour of `marks` + `markGroups`. ⚠️ Still **no θ and no 0–5 level** on this page — that stays maths-only. **UI only — no data, query, rule or API change.**
- 2026-08-03 — `lib/MilliyQuiz.ts` (`MILLIY_SUBJECTS`), `types/MilliyQuiz.ts` (`MilliySubjectId`), `lib/MilliyDefaultPaper.ts` (`SAMPLE_FILES`), `data/question_topics.json`, `data/physics-default-paper.json` (new), `data/ona-tili-default-paper.json` (new), `scripts/addPhysicsTopics.mjs` (new), `scripts/addNativeLanguageTopics.mjs` (new), `app/teacher/milliy-sertifikat/page.tsx`, `lib/announcements.ts`, `docs/MILLIY_QUIZ.md`: **two more Milliy sertifikat subjects are buildable — `fizika` (flipped `soon` → `generic`) and the NEW `ona-tili`.** ⚠️ `MilliySubjectId` is now `'biologiya' | 'kimyo' | 'fizika' | 'ona-tili' | 'ingliz'` — widen the enum, and `ona-tili` is the **first id with a hyphen** (route segment == stored `subject` == taxonomy slug; do not normalize it to `ona_tili`). Registry defaults: fizika **35 questions / 100 min**, ona tili **30 / 90** — ⚠️ these are THIS REPO's numbers matching their sample papers, **not** a published protocol (only maths has a blueprint), so no surface may present them as a DTM count. `data/question_topics.json` gained two subjects — `fizika` (5 topics / 98 subtopics, groups folded into subtopic names as `"Group — Leaf"`) and `ona-tili` (7 topics / 84 subtopics, **language only, deliberately no literature topic**) — which also makes both authorable in `/teacher/create/question` and `/teacher/create/block`; mirror the JSON verbatim, the ids are persisted into `teacher_questions`. Two new sample papers ship behind "Namunaviy variant" (physics 35: 30 closed + 5 `numeric: true` typed; ona tili 30: 26 closed + 4 TEXT-graded typed). ⚠️ **Ona tili typed answers must stay apostrophe-free** — `textMatch` collapses case/whitespace but not apostrophes, so `ko'makchi` would fail on a different glyph. Nothing else moved: no new component, route, service, rule or index. **No Firestore shape, query, rule or API contract change** — only the value space of `milliy_quizzes.subject` widened.
- 2026-09-14 — `lib/SatDefaultPaper.ts` (new), `data/sat-math-default-paper.json` (new), `public/sat/*.png` (17 new static images), `app/teacher/sat/math/build/page.tsx`, `docs/SAT_QUIZ.md`: **the SAT Math builder gained a "Namunaviy variant" sample test** — one button in the "Savol qo'shish" card that fills all three modules (Module 1 / Module 2 Easier / Module 2 Harder) at once from a bundled REAL 66-question sample (22 per module, the real digital SAT's own module length), mirroring `lib/MilliyDefaultPaper.ts`'s pattern. Curated from a donated 885-question Algebra bank: Module 2 Easier is entirely beginner/easy/medium items, Module 2 Harder entirely hard/expert, Module 1 a genuine mix; 17 of the 66 carry a graph image, served as a bundled STATIC file under `public/sat/` (root-relative `/sat/<id>.png`) — deliberately NOT a Firebase Storage URL, so the sample stays a zero-Firestore-read, zero-network-write bundle even with images. All 66 are `mcq` (the source bank had no `numeric` items). Replaces every module's contents (confirm prompt if any is non-empty) and also fills `module1Minutes`/`module2Minutes`/`routingThreshold` from the file if the teacher hasn't touched them. Sample items use ids `sat-sample-<module>-NN`, never in a real question bank. **No Firestore shape, query, rule or API contract change** — pure client-side data + a dynamic `import()`, same `SatQuizItem` shape every hand-picked item already has. (An earlier draft of this entry described importing the bank into `teacher_questions` via Admin SDK — that path was abandoned in favor of this fully bundled, program-local approach; no Firestore writes were made.)
- 2026-09-14 — `hooks/useCenterClasses.ts` (`ClassData.schoolClassId`/`schoolClassName`), `firestore.rules` (new `school_classes` collection), `docs/MANAGER.md`: **informational only, no teacher-app work.** The manager panel's Groups page was redesigned into grade-based "School Classes" (e.g. "5-A") containing several subject groups with a shared roster — see `ANDROID_IMPACT.md` for the manager-side contract. The ONLY thing that reaches a `classes` doc a teacher reads is two new **additive, optional** fields, `schoolClassId`/`schoolClassName` — `app/teacher/center` and every other teacher surface can keep ignoring them; nothing about roster/schedule/attendance/grading semantics changed for a teacher's own group. Logged only because the ledger tracks every Firestore shape change.
- 2026-09-15 — `types/SatQuiz.ts` (`SatRwDomain`/`SatDomain`), `lib/SatMathQuiz.ts` (`SAT_RW_DOMAINS`/`SAT_PLATFORM_CREATOR_ID`/`isSatEnglishAuthorableType`, `SAT_SECTIONS.reading-writing` flipped `soon`→`live`), `services/satEnglishQuizService.ts` (new), `app/teacher/sat/english/*` (new: list, build, results, `SatEnglishBankPicker`, draft store), `scripts/addSATEnglishTopics.mjs` (new), `scripts/importSATEnglishQuestions.ts` (new, already run), `firestore.rules` (new §12e), `firestore.indexes.json`, `docs/SAT_QUIZ.md`, `docs/QUESTIONS.md`: **SAT English (Reading & Writing) is now a live, buildable section**, a sibling of SAT Math in its OWN Firestore collections (`sat_english_tests`/`sat_english_results`, own 6-digit code namespace, never cross-checked against `sat_math_tests`). New taxonomy `sat-ingliz-tili` — 4 official College Board R&W domains, each with exactly ONE subtopic (the question bank carries no finer content-category tag) — mirror `data/question_topics.json` verbatim, ids persist into `teacher_questions`. ⚠️ **New question-bank concept: PLATFORM-owned content.** `teacher_questions` rules are `allow read: if isAuth()` for every signed-in user (not creator-scoped) — a one-off Admin-SDK script imported a donated 1,443-question bank (de-duped by content to 1,406 unique; a trailing-meta-question artifact in 53% of source rows was stripped before import) as `teacher_questions` docs with `creatorId: ''` (the SAME "no owner" sentinel `sat_math_tests.teacherId` already used), `creationMethod: 'exam_import'`, `status: 'published'`. `SatEnglishBankPicker`'s "Platform bank" tab queries `creatorId == ''` instead of the signed-in uid — same existing `creatorId + subject.id + createdAt desc` index, no rules change. Mirror this as a read-only, always-available question source alongside a teacher's own bank when building an English module — never let a teacher write, edit or delete a platform-owned question. SAT English authors ONLY `mcq` (no grid-in, unlike Math). No bundled sample/starter test for English (unlike Math's "Namunaviy variant") — the platform bank already gives 1,406 ready questions. **Firestore shape is additive-only**: `SatQuizItem.domain` widened to accept either subject's domain id, `SatMathResult.domains` key widened the same way — an existing Math document's shape is unaffected.
- 2026-09-15 — `firestore.rules` (new `isCenterOffice()` helper; `center_teachers` `get`/`list` gained an `|| isCenterOffice(resource.data.centerId)` branch; new `center_staff` + `center_staff_credentials` blocks; widened READ rules on the finance collections), `app/teacher/layout.tsx` (role redirect only), `services/userService.ts` (`UserProfile.role` union), `docs/OFFICE.md` (new): **informational only, no teacher-app work.** Two new center back-office roles (director / buxgalter) got their own web panel at `/office` — full contract in `ANDROID_IMPACT.md`. ⚠️ **A teacher's own access is unchanged in every branch**: the `center_teachers` rules only gained an EXTRA `||` arm for office staff, so `teacherId == request.auth.uid` and the manager arm behave exactly as before, and `GET /api/teacher/payroll` (the Maosh tab) was not touched. The only teacher-tree change is one more branch in the layout's role guard (`director`/`accountant` → `/office`); `users.role` remains a **redirect hint, never an authorization source**. ⚠️ Note for the salary screen: a center's director and buxgalter can now READ `center_payouts` for their center, so an approved payout is visible to them as well as to the teacher — no shape change, but worth knowing if the Android teacher app ever words that screen as "only you can see this". Logged because the ledger tracks every `firestore.rules` change.
- 2026-09-16 — `lib/SATscore.ts` (rewritten — see `ANDROID_STUDENT_IMPACT.md` for the full scoring contract), `types/SatQuiz.ts` (`SatMathResult.scoreBand?`, `SatMathResult.omitted?`), `app/teacher/sat/math/results/[testId]/page.tsx`, `app/teacher/sat/english/results/[testId]/page.tsx`, `docs/SAT_QUIZ.md`: **SAT scaled scores now come from the real published conversion curve and are reported as a BAND; the per-student row gained a wrong-vs-blank breakdown.** ⚠️ If the Android teacher app renders SAT results, mirror: the per-student row shows `formatScoreBand(r.scoreBand)` (e.g. "640–700") when the sitting has a band and falls back to the single `r.scaledScore` when it does not, and the expanded detail gains a "Breakdown / Tahlil / Разбор" cell reading `total − correct − omitted.length` wrong · `omitted.length` blank — **rendered ONLY when `omitted !== undefined`**, never as "0 blank" on an older sitting that predates the field. Both `scoreBand` and `omitted` are additive and optional; `items` is unchanged (still binary 0/1, which is why blanks needed their own field). ⚠️ The stat tiles (average/best scaled score) now mix two scoring models on any test with sittings from both sides of 2026-09-16 — nothing backfills old scores, and that is deliberate. Teacher-side grading, the domain bars and the per-question solve-rate grid are all unchanged.
- 2026-09-16 — `data/question_topics.json` (`sat-matematika` subtopics REPLACED), `scripts/setSATMathSubtopics.mjs` (new), `lib/SatQuestionImport.ts` (new), `app/teacher/sat/import/page.tsx` (new), `app/teacher/sat/page.tsx` (entry card), `app/teacher/sat/math/_components/SatBankPicker.tsx`, `app/teacher/sat/english/_components/SatEnglishBankPicker.tsx`, `app/teacher/sat/{math,english}/build/page.tsx` (picker strings), `services/questionBankService.ts` (`fetchSharedQuestionsPage`), `types/question.ts` (`QuestionV1.sharedBank?`), `firestore.indexes.json` (1 new index), `docs/SAT_QUIZ.md`, `docs/QUESTIONS.md`: **(1) the SAT Math taxonomy's SUBTOPICS changed, and (2) teachers can now bulk-import a SAT question dataset from a JSON file.**
  ⚠️ **(1) MIRROR THE TAXONOMY VERBATIM from `data/question_topics.json`.** The 4 `sat-matematika` TOPIC ids and names are UNCHANGED (`algebra`, `advanced-math`, `problem-solving-and-data-analysis`, `geometry-and-trigonometry`) — they are also `SatMathDomain` values and keys inside `SatMathResult.domains`, so they must never be re-slugged. Only the SUBTOPIC level moved: the official College Board content categories were swapped for the centre's own book sections, now 25 across the four (6/6/7/6). **17 old subtopic ids are now orphans** on already-saved questions — they still display and grade (the stored NAME renders), they just match no taxonomy row; only `linear-functions` and `circles` survive. Nothing is migrated. If the Android teacher app ships a copy of the taxonomy, re-copy the whole `sat-matematika` subject.
  ⚠️ **(2) New OPTIONAL Firestore field on `teacher_questions`: `sharedBank?: boolean`** — a flat, queried VISIBILITY flag, absent on every pre-existing doc (reads as "not shared"). Set by the importer when the teacher opts in. **It is NOT an ownership change**: `creatorId` stays the uploader's uid (the create rule `creatorId == request.auth.uid` forbids anything else), so only they can edit/delete it and the picker shows their `creatorName` as attribution. Do NOT confuse it with `creatorId: ''` (`SAT_PLATFORM_CREATOR_ID`), the anonymous Admin-SDK-only platform pool — a client cannot write that and must not try. **New composite index** `teacher_questions: sharedBank ASC, subject.id ASC, createdAt DESC` backing `fetchSharedQuestionsPage(subjectSlug, …)`; it deliberately cannot ride the existing `creatorId + subject.id + createdAt` index because the query is not creator-scoped. Deploy indexes before shipping any client that runs it. Import writes go through the normal client SDK in `writeBatch` chunks of 500 (Firestore's cap) — **no API route, no rules change**; every row is built by `toQuestionV1` with `creationMethod: 'exam_import'`, which defaults status to **`review`** (deliberately not `published`). Rows are validated against the taxonomy BEFORE any write, because `toQuestionV1` throws `InvalidTopicError` on an unknown subject/topic/subtopic triple and a mid-import throw would leave earlier rows committed. File format, the wrapper/array tolerance, the 500-row cap and the per-row rejection reasons all live in `lib/SatQuestionImport.ts` (`SAT_IMPORT_EXAMPLE` is the single source of truth the UI also prints) — mirror that module rather than re-deriving the format. Picker changes to mirror: SAT Math's picker gained a BANK axis (`My bank` / `Shared`) that is separate from its existing provenance chips (all/AI/me), and those chips hide while `Shared` is selected; SAT English's picker gained `Shared` as a third source beside `Platform bank` and `Me`. Shared rows display the uploader's name.
